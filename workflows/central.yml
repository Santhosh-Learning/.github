name: Org Test

on:
  workflow_dispatch

jobs:
    org:
        runs-on: ubuntu-latest
        environment:
          name: production
        steps:
          - name: Print Org test message
            run: echo "Successful"